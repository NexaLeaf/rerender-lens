# rerender-lens example

Vite + React 19 app with three deliberate re-render bugs. From the repo root:

```sh
npm install
npm --prefix examples/vite-react install
npm run dev:example
```

Open http://localhost:5199, open the console, type in the search box or press "Re-render App".
